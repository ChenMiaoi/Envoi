# README screenshots

Captured from the Envoi v0.2.0 desktop build on Windows using the graphite theme and a separate application profile.

- `writing.jpg`: the bundled example compiled locally, with the evaluation chapter selected in Writer.
- `experiments.jpg`: a new example experiment alongside the main workspace, with a demonstration commit expanded.

These are unretouched application screenshots. The example manuscript, data and Git history are demonstrations; no real research results or AI conversations are represented. Refresh screenshots from an independent example copy, not a personal research project.
