# Synthetic model data
`model-data.csv` is generated by `../../../app/scripts/generate-demo-figures.py`, not collected from a GPU. Each of 21 rows stores sequence length, head dimension, operation count, bytes, component and total times in seconds, and workspace bytes. Regenerate the table and figures together to keep all outputs consistent.
